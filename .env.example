# ================================================
# [로컬 Windows - RTX 4060 8GB] 기본 설정
# 실행: copy .env.example .env  → python run_watchdog.py
# ================================================

# --- 경로 (Windows) ---
PROJECT_DIR=C:\My_Digital_Persona_Own_LLM_Project
RAW_DATA_DIR=G:\
QDRANT_PATH=C:\My_Digital_Persona_Own_LLM_Project\qdrant_db
STATE_DB_PATH=C:\My_Digital_Persona_Own_LLM_Project\processed_files.db
LOG_FILE=C:\My_Digital_Persona_Own_LLM_Project\ingestion_log.txt
BACKUP_ROOT=C:\My_Digital_Persona_Own_LLM_Project\backups

# --- poppler (Windows 필수) ---
# https://github.com/oschwartz10612/poppler-windows/releases 에서 설치 후 bin 폴더 경로 입력
POPPLER_PATH=C:\Users\<your-username>\Downloads\poppler-25.x.x\Library\bin

# --- OCR / 모델 ---
DPI=150
VISION_MODEL=qwen3-vl:8b
WHISPER_MODEL=base
# RTX 4060 8GB: Ollama(~8GB) + Whisper 병행 → small로 VRAM 절약
EMBED_MODEL_NAME=intfloat/multilingual-e5-small
CHUNK_SIZE=1200
CHUNK_OVERLAP=200

# --- 병렬 처리 (RTX 4060 기준 최적값) ---
MAX_VISION_WORKERS=2
MAX_AUDIO_WORKERS=1
QUEUE_SIZE=100
AUTO_BACKUP_INTERVAL_SEC=900

# --- Ollama 엔드포인트 ---
# 로컬 실행 시 둘 다 localhost
CLOUD_OLLAMA_URL=http://localhost:11434
LOCAL_OLLAMA_URL=http://localhost:11434
